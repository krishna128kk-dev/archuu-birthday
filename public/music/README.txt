Put your chosen song here as:
  song.mp3

That's it — the music button in the top-right corner of the birthday
experience will automatically use this file once she interacts with the page
(browsers block autoplay, so a manual button is used instead).
